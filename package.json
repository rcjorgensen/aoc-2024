{
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "serve": "http-server",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.4.1",
    "typescript": "^5.7.2"
  }
}
